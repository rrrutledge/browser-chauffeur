Follow the drainer engine/worker-core.md. Your item is items/<id>.json (plus any body file it points
to); its "source" field names the provider. Read providers/<source>-provider.md for that source's
CLEAR (advance) and DRAFT-MODE steps.

Draft any reply via the message-draft skill in the provider's DRAFT-MODE (draft-only, never send;
for Teams, honor its footguns — identity-gate the chat, visible composer, Shift+Enter, never a bare
Enter). Do the work first; ADVANCE via the provider's CLEAR only once it's done, and only after the
user confirms any send. If nothing is needed right now, resolve quietly.

Your FINAL message before yielding MUST open with the briefing (who / what they asked / any deadline)
and then what you did — the user may never have seen your earlier messages. Write items/<id>.done last.
